import type { MobileConfig } from "../types/config";

/**
 * Mobile-only configuration.
 *
 * Drives components under src/components/mobile/ (features that desktop never
 * loads). Reserved for future use — mobile currently uses a static banner.
 */
export const mobileConfig: MobileConfig = {
	// Add mobile-specific options here as they are introduced.
};
